import 'package:flutter/material.dart';

// a primary color definition
const primaryColor = Color.fromARGB(255, 123, 46, 46);
const primaryLightColor = Color(0xFFFFECDF);
const primaryGradientColor = LinearGradient(
  colors: [
    Color(0xFFFFA53E),
    Color(0xFFFF7643),
  ]);

  // a secondary color definition
  const secondaryColor = Color(0xFF979797);
  const textColor = Color(0xFF757575); 

  // animationDuration time definition
  const animationDuration = Duration(milliseconds: 200);

// const untuk nyimpan value yg tetap dan gk mau diubah2
// final  keyword untuk nyimpan library yg udah tetap

// default constant value for padding size
const defaultPadding = 20.0;
